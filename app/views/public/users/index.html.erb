<h2>ユーザー一覧</h2>

<% @users.each do |user| %>
  <%= link_to user_path(user.id) do %>
    <%= image_tag user.get_profile_image(200,200) %>
    <h3><%= user.name %></h3>
  <% end %>
<% end %>

<%= paginate @users %>